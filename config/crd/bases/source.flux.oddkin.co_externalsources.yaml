---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: externalsources.source.flux.oddkin.co
spec:
  group: source.flux.oddkin.co
  names:
    kind: ExternalSource
    listKind: ExternalSourceList
    plural: externalsources
    singular: externalsource
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].message
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ExternalSource is the Schema for the externalsources API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ExternalSource
            properties:
              destinationPath:
                description: DestinationPath specifies the relative path within the
                  artifact where the data should be placed
                type: string
              generator:
                description: Generator specifies the source generator configuration
                properties:
                  http:
                    description: HTTP specifies HTTP generator configuration
                    properties:
                      caBundleSecretRef:
                        description: CABundleSecretRef references a secret containing
                          a CA bundle for TLS verification
                        properties:
                          key:
                            description: Key within the secret
                            type: string
                          name:
                            description: Name of the secret
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      headersSecretRef:
                        description: HeadersSecretRef references a secret containing
                          HTTP headers
                        properties:
                          name:
                            description: Name of the secret
                            type: string
                        required:
                        - name
                        type: object
                      insecureSkipVerify:
                        description: InsecureSkipVerify skips TLS certificate verification
                          (not recommended for production)
                        type: boolean
                      method:
                        default: GET
                        description: Method specifies the HTTP method to use
                        type: string
                      url:
                        description: URL is the HTTP endpoint to fetch data from
                        format: uri
                        type: string
                    required:
                    - url
                    type: object
                  type:
                    description: Type specifies the generator type
                    enum:
                    - http
                    type: string
                required:
                - type
                type: object
              interval:
                description: Interval specifies the reconciliation frequency
                minLength: 2
                pattern: ^([0-9]+(\.[0-9]+)?(ms|s|m|h))+$
                type: string
              suspend:
                description: Suspend tells the controller to suspend reconciliation
                  for this ExternalSource
                type: boolean
              transform:
                description: Transform specifies optional data transformation
                properties:
                  expression:
                    description: Expression contains the transformation expression
                    type: string
                  type:
                    description: Type specifies the transformation engine type
                    enum:
                    - cel
                    type: string
                required:
                - expression
                - type
                type: object
            required:
            - generator
            - interval
            type: object
          status:
            description: status defines the observed state of ExternalSource
            properties:
              artifact:
                description: Artifact contains information about the current artifact
                properties:
                  lastUpdateTime:
                    description: LastUpdateTime is when the artifact was last updated
                    format: date-time
                    type: string
                  metadata:
                    additionalProperties:
                      type: string
                    description: Metadata contains additional artifact metadata
                    type: object
                  revision:
                    description: Revision is the content-based revision of the artifact
                    type: string
                  url:
                    description: URL is the location where the artifact can be accessed
                    type: string
                required:
                - lastUpdateTime
                - revision
                - url
                type: object
              conditions:
                description: Conditions represent the current state of the ExternalSource
                  resource
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledETag:
                description: LastHandledETag contains the ETag from the last successful
                  fetch (for HTTP sources)
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation of
                  the ExternalSource
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
